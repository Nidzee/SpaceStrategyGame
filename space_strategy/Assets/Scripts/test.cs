using UnityEngine;

public class test : MonoBehaviour
{
    private GameObject uiImageRef;

    private int counter = 0;

    private Vector2 screenCenter = new Vector2(Screen.width/2, Screen.height/2);

    // private void Update()
    // {
    //     if (counter < 3)
    //     {
    //         counter++;

    //         GameObject.Instantiate(uiImageRef, screenCenter, Quaternion.identity);
    //     }
    // }
}
